apiVersion: v1
kind: Service
metadata:
  name: {{ include "sylvanclash-frontend.fullname" . }}
  labels:
    {{- include "sylvanclash-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sylvanclash-frontend.selectorLabels" . | nindent 4 }}
